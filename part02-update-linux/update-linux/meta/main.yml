galaxy_info:
  author: Emran
  description: Update Linux machine
  license: MIT
  min_ansible_version: 2.1

  platforms:
    - name: Redhat Family
    - name: Debian Family

  galaxy_tags:
    - linux
    - apt
    - apt-get
    - dnf
    - yum

dependencies:
  # List other roles that this role depends on, for example:
  # - { role: common, src: git://github.com/example/common.git }

